/** OPS ≈ OBP × 1.8 on average. Used to approximate OPS when only OBP is available. */
export const OBP_TO_OPS_SCALE = 1.8;

/** wOBA ≈ OPS / 1.8 on average. Used to approximate wOBA when only OPS is available. */
export const OPS_TO_WOBA_SCALE = 1.8;
